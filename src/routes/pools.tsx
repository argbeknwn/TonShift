import { Text } from '@chakra-ui/react';
import { memo } from 'react';

const PoolsRoute = memo(() => {
  return <Text>{'PoolsRoute'}</Text>;
});

PoolsRoute.displayName = 'PoolsRoute';

export { PoolsRoute };
